# CI Security Pipeline Lab — small Python app

This repository contains a tiny Python app plus CI which:
- Runs unit tests with pytest
- Runs Bandit (SAST)
- Runs Semgrep (SAST)
- Runs Safety (SCA)
